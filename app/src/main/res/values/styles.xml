<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="CardItem">
        <item name="android:paddingLeft">@dimen/weather_card_horizontal_margin</item>
        <item name="android:paddingRight">@dimen/weather_card_horizontal_margin</item>
        <item name="android:paddingBottom">@dimen/weather_card_vertical_margin</item>
        <item name="android:textSize">12sp</item>
    </style>

    <style name="CardFirstItem" parent="CardItem">
        <item name="android:paddingTop">@dimen/weather_card_vertical_margin</item>
    </style>

</resources>
